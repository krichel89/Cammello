PyQt5>=5.15.0
requests>=2.28.0
Pillow>=9.0.0
# 03.08.2026: OBERGRENZE, nicht kosmetisch. pyexiv2 2.16.0 (erschienen
# am 02.08.2026) laesst den Windows-Build von PyInstaller abstuerzen:
# beim Ermitteln der Bibliotheksabhaengigkeiten importiert PyInstaller
# jedes eingesammelte Paket in einem eigenen Prozess, und der Import
# von pyexiv2.lib (laedt exiv2.dll per ctypes) endet mit
# 0xC0000005 (Zugriffsverletzung). Mac und Linux sind NICHT betroffen -
# der 0.16.1-Release hat dort mit 2.16.0 sauber gebaut.
# Vor dem Anheben: einen Windows-Build mit der neuen Version testen.
pyexiv2>=2.15,<2.16
paramiko>=3.0
rawpy>=0.27
exifread>=3.0
keyring>=24

# 0.18.3 - Import direkt von der Kamera (PTP). Canon-Bodies melden sich
# nicht als Massenspeicher, die Karte wird also nie gemountet.
# NUR macOS und Linux: libgphoto2 ist nie nach Windows portiert worden
# (gphoto/libgphoto2#279), auf Windows gibt es kein Wheel und pip wuerde
# aus dem Quelltext bauen wollen und den Build umbringen -> Marker.
# Die Wheels bringen libgphoto2 samt camlibs/iolibs mit und setzen
# CAMLIBS/IOLIBS selbst relativ zum Paketverzeichnis; PyInstaller braucht
# darum --collect-all gphoto2, sonst fehlt ptp2.so im Bundle.
# Obergrenze gesetzt (Lehre aus dem pyexiv2-2.16-Vorfall).
gphoto2>=2.6,<2.7 ; sys_platform != "win32"
